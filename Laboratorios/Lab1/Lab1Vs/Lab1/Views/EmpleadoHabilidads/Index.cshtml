@model IEnumerable<Lab1.Models.EmpleadoHabilidad>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreHabilidad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdEmpleadoNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreHabilidad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdEmpleadoNavigation.IdEmpleado)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdHabilidad">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdHabilidad">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdHabilidad">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
